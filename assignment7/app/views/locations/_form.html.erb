<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h3><div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div></h3>
  <h3><div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div></h3>
  <%= f.hidden_field :event_id, :value => @location.event_id.nil? ? params[:event_id] : @location.event_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
